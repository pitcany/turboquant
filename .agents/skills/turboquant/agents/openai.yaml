interface:
  display_name: "Turboquant"
  short_description: "Repo-specific patterns and workflows for turboquant"
  default_prompt: "Use the turboquant repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true